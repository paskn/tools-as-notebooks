{
  "name": "Python & R Course Environment",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",
  "features": {
    "ghcr.io/rocker-org/devcontainer-features/r-rig:1": {
      "version": "latest"
    }
  },
  "customizations": {
    "vscode": {
      "settings": {
        "r.rterm.linux": "/usr/local/bin/R",
        "python.defaultInterpreterPath": "/usr/local/bin/python"
      },
      "extensions": [
        "ms-python.python",
        "ms-toolsai.jupyter",
        "REditorSupport.r"
      ]
    }
  },
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y cmake libgit2-dev libzmq3-dev pandoc && Rscript -e 'install.packages(c(\"renv\", \"languageserver\"), repos=\"https://cloud.r-project.org\")' && Rscript -e 'renv::restore(prompt = FALSE)'"
}
